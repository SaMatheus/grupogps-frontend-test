import React from 'react';

// COMPONENTS
import MagicTable from '../../components/MagicTable';

const Home = () => {
  return (
    <MagicTable>
      <button>Começar</button>
    </MagicTable>
  );
};

export default Home;
